<?php

return [
    /*
    |--------------------------------------------------------------------------
    | Banner Visibility
    |--------------------------------------------------------------------------
    |
    | Controls whether the homepage banner is displayed. Set to false to hide
    | the banner entirely without removing the content.
    |
    */
    'visible' => true,

    /*
    |--------------------------------------------------------------------------
    | Banner Content (HTML)
    |--------------------------------------------------------------------------
    |
    | The HTML content rendered inside the homepage banner. This can include
    | inline markup such as <span>, <strong>, <a>, etc.
    |
    */
    'content' => 'LIMITED OFFER: Save 25% on your first month with coupon code <span class="coupon">FIRST25PC</span> at checkout.',
];

